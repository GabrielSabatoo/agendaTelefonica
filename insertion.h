#ifndef INSERCAO_H
#define INSERCAO_H
#include "listaEstatica.h"
#include "AgendaTel.h"
#include <string.h>

void insercaoNome(LIST *l, int n);

void swap(contato *i, contato *j);

void bubble(LIST *l, int n);
#endif
